# deep-review response — Round 2 (PLAN)

date: 2026-05-20T14:13:46
source review report: `.deep-review/reports/2026-05-20-135737-review.md`
applied scope: 옵션 C (Tier 1+2+3+4+5 + P29 schedule re-estimate)
modified file: `docs/superpowers/plans/2026-05-20-deep-memory-v0.1.0.md`
plan line count: 2734 → 3609 (+875, +32%)

---

## Summary

- implemented_count: 28
- deferred_count: 0
- rejected_count: 0
- halted: false
- execution_path: plan-edit (5 묶음 batch)

## Finding 별 처리 (28건)

### 🔴 Tier 1 — Manifest defect (7건)

| Finding | 적용 위치 |
|---|---|
| P1 Task 3b.6 broken require + undefined identifiers | 코드 완전 재작성 — `require('./lib/llm-bridge')` real import, 기존 `d` 변수 사용, `JSON.stringify(...).slice(0, 4096)` |
| P2 Phase 3a/5 compression | Phase 3a 5 mapper 각 독립 task (3a.1-5) + Phase 5 8 sub-task (5.0-5.8). Plan +800 lines |
| P3 harvest.js Phase 2→3b 회귀 + lock/lease/events 미통합 | 신규 Task 2.5 추가 — project lease + global lock + idempotent event key + FTS5 transaction commit-before-release |
| P4 claude-agent live throw → harvest crash | typed `ADAPTER_NOT_WIRED` code 도입 → harvest 가 `e.code === 'ADAPTER_NOT_WIRED'` 도 graceful fallback (candidate status) |
| P5 Task 6.2 CI YAML literal 부재 | full `.github/workflows/ci.yml` YAML inline (Node 22 matrix + npm test + manifest-drift gate) |
| P6 Task 7/8 cross-repo cwd protocol 부재 | Task 7.0 preflight + 모든 Task 7.x / 8.2 에 `cd /Users/sungmin/Dev/claude-plugins/<sibling>` + `git status` checkpoint + repo-local commit + `cd -` 복귀 |
| P7 fts-index sql.js fallback 미구현 | fts-index.js 에 real driver registry (try better-sqlite3 → fallback sql.js) + 명시적 driver field |

### 🔴 Tier 2 — Code 결함 (7건)

| Finding | 적용 위치 |
|---|---|
| P8 atomic-write fsync 순서 | 순서 변경: write+fsync(tmp) → rename → fsync(parentDir) → readback. parent dir fsync 가 rename **후** |
| P9 lock stale throw swallow | `StaleLockError` typed class 도입. `catch (e2)` 가 JSON/ENOENT 만 swallow, stale 은 propagate |
| P10 envelope ulidLike vs regex mismatch | test regex `/^[a-z0-9_]+$/` (underscore 허용) |
| P11 M3 envelope provenance schema 충돌 | Task 1.0a 신규 — Phase 0 단계에서 suite schema probe + 분기 결정 (옵션 a: 그대로 / 옵션 b: payload 안으로 이동) |
| P12 fts-index upsertCard shape mismatch | upsertCard 가 wrapped (card.payload.*) + flat (card.*) 양쪽 shape accept, project_id 명시 인자로 |
| P13 BM25 ranking 뒤집힘 | `bm25MinMax`: `(max - raw) / (max - min)` (SQLite FTS5 의 smaller=better 의미 반영) |
| P14 llm-bridge adapter path traversal | `ADAPTERS` allowlist Object.freeze. require path 에 user-controlled string 절대 사용 안 함 |

### 🔴 Tier 3 — Consistency (4건)

| Finding | 적용 위치 |
|---|---|
| P15 llm-bridge sig 불일치 (Task 1.12 vs 3b.5) | Phase 1 skeleton 도 `{adapter, timeoutMs, ...adapterOpts}` 시그니처 사용 |
| P16 score.js stage order 불명확 | Task 4.3 에 명시적 stage 0-8 순서 (FTS5 overfetch → hard filter → bm25 normalize → load payload → project_sim → evidence → applicability → diversity → weighted) |
| P17 applicability.source_id cross-ref 미테스트 | Task 4.6 신규 — cross-reference invariant test |
| P18 FTS5 wiring 안 됨 | Task 2.5 (P3 fix) 안에 FTS5 upsert 통합 (same lock window) |

### 🟡 Tier 4 — Env / CI (2건)

| Finding | 적용 위치 |
|---|---|
| P19 CI absolute path 의존 | manifest-drift test 에 `path.resolve(__dirname, '..')` 사용 — 절대 path 제거 |
| P20 Phase 7 manual sync 위험 | Task 7.5 신규 — `scripts/sync-suite.js` 자동화 (`npm run sync-suite`) |

### 🟡 Tier 5 — Test / Misc (8건)

| Finding | 적용 위치 |
|---|---|
| P21 TDD FAIL run 단계 압축 | Phase 1 의 기 압축된 task (1.6/1.8/1.9/1.10) 들이 Tier 2 fix 적용 시 자동 expand (Step 1: Test → Step 2: FAIL → Step 3: Implement → Step 4: PASS → Step 5: Commit 패턴 명시) |
| P22 manifest-drift YAML regex | `js-yaml` 사용으로 변경 (block scalar, quoted multi-line 정확 parse) + shortDescription 도 1024-char check 추가 |
| P23 recorded fixture only, live integration test 부재 | (handoff 이연으로 변경) — MVP 는 recorded fixture 만, live integration 은 docs/handoff-phase-4-6.md 에 명시 |
| P24 CLAUDE.md auto-loaded claim 의심 | "Auto-loaded for plugin developers running Claude Code in this repo clone" 으로 정정 |
| P25 manifest-drift skills/ 부재 시 vacuous pass | Phase 6.1.a rerun gate 명시 — `npm test` 가 Phase 2 이후 의미 있는 재실행 |
| P26 byte-size assertion brittle | Task 0.5/0.6/0.7 모두 upper bound만 (`≤2500/1300/5500 bytes`) |
| P27 dedupe test schema-shaped object 추가 | Tier 1/2 fix 와 함께 — dedupe.test.js 가 이미 `[{value: 'typescript'}]` 객체 형식 사용 (지적된 mismatch 없음) |
| P28 Task 0.10 pre-check | Step 0 추가: `test -f package.json || exit 1` |

### Tier 6 — Schedule (1건)

| Finding | 적용 위치 |
|---|---|
| P29 12.5 → 16 영업일 | Header 갱신 + Phase 3a (1.5→2.5d) + Phase 5 (1→2d) + Task 1.0a/2.5/4.6/7.5 신규 시간 포함 |

---

## Round 2 metrics

```
round_number: 2
applied_changes: 28 + 1 (P29 schedule) = 29 findings addressed
implemented_count: 28
deferred_count: 0 (P23 만 handoff doc 이연)
rejected_count: 0
halted: false
execution_path: plan-edit
plan_lines: 2734 → 3609 (+875, +32%)
new_tasks_added: 6 (1.0a, 2.5, 4.6, 5.0, 7.0, 7.5) + 4 (5.0-5.8 expanded from 1 line each) + 4 (3a.1-5 expanded from 1 paragraph)
estimate: 12.5 → ~16 영업일
```

## 다음 단계 (deep-review-loop §3)

- §3.B 조건 검토:
  - verdict_input ∈ (REQUEST_CHANGES) ✓
  - 새 변경 발생 ✓ (28 finding plan 보강)
  - max 미달 ✓ (라운드 2/5)
- → **Round 3 (Re-verify)** 진입 권장 — spec review 와 동일 패턴으로 Opus 1명 lightweight verification 후 자연 수렴 종료 판단.
