# deep-review response — Round 2

date: 2026-05-20T13:27:56
source review report: `.deep-review/reports/2026-05-20-123352-review.md`
applied scope: P1+P2+P3+P4+P5 + 핵심 yellows (옵션 C, 사용자 선택)
modified file: `docs/superpowers/specs/2026-05-20-deep-memory-design.md`

---

## Summary

- implemented_count: 21
- deferred_count: 4 (handoff doc 이연)
- rejected_count: 0
- halted: false
- execution_path: spec-edit (코드 변경 없음, 본 라운드는 spec doc 자체 수정)

## Finding 별 처리

### 🔴 critical (14건) — 100% 적용

| Finding | 적용 위치 | 형태 |
|---|---|---|
| F1 claim origin contradiction | §7.1 매핑 표 + invariant | 5 memory_type 모두 deterministic claim source. empty-claim quarantine 정책. |
| F2 avoid_when ↔ non_applicability mismatch | §9.2 field mapping 표 + per-card fallback | `avoid_when ← non_applicability[].value` 명시. 빈 값 = `"(none specified)"` 렌더, brief fail 차단. |
| F3 atomic write + lock recovery | §7.3 + §15 Phase 1 + D18 | temp+fsync+rename, lock metadata `{pid, host, created_at}`, stale detect (>5min), `--unlock` 명령 MVP 포함. |
| F4 concurrent harvest idempotency | §7.3 + tests/concurrent-harvest | project lease + event key sha256(path+content_hash+run_id), 같은 key skip. |
| F5 LLM output schema validation | §7.2 + D16 schemas | `schemas/memory-card-distill-output.schema.json` Ajv strict, 위반 = candidate fallback. |
| F6 pre-LLM redaction gap | §11.1 3-pass + D17 | Pass 1 (Step A in) / Pass 2 (Step B in) / Pass 3 (wrap 직전). evidence_paths home segment hash. |
| F7 score formula scale | §8 정규화 명세 + tests/score-normalization | BM25 min-max scaling, evidence_quality sigmoid clamp, stale_penalty clamp 0-1. zero/all-stale/missing-profile degraded path. |
| F8 source provenance brittleness | §6.1 + tests/source-hash | `source_artifacts[].content_hash` + `captured_at` + `artifact_kind` + `schema_version`. audit `unresolved-source` 리포트. |
| F9 global promotion gate | §6.1 (privacy_level 필드) + §6.3 + D15 | local 기본, `--promote <id>` 명시 호출만 (자동 X). 디렉토리 분리 (`cards/<type>/{global, project_id}/`). |
| F10 NFS/symlink/read-only preflight | §5 + §15 Phase 2 + preflight.js | realpath/writability/fsync/sqlite probe. `--allow-network-root` 옵션. read-only → brief-only degrade. |
| F11 cross-runtime sub-agent bridge | §7.2 + D19 + adapter-registry.js | `llm-bridge.js` 추상화, 5 adapter (auto / claude-agent / codex-bash / gemini-sdk / stdin-fallback). |
| F12 runtime contract tests | §12 + tests/runtime-contract/ | adapter 별 smoke fixture + recorded golden hash 비교. |
| F13 sqlite Q2 deferred | D16 + §15 Phase 0 + §17 | better-sqlite3 pin + Phase 0 install probe + sql.js fallback. Q2 spec 으로 승격. |
| F14 Phase 3 unrealistic | §15 Phase 3 → 3a + 3b 분리 + D14 | 3a (rule, 1.5일) + 3b (sub-agent + bridge + harness, 2.5일). 총 9 → 12.5 영업일. |

### 🟡 nice-to-have — 7건 적용, 4건 handoff 이연

**적용 (7건)**:
| Finding | 적용 위치 |
|---|---|
| F15 dedupe normalize 보수화 | §6.2 — alphanumeric-only, stop-word 제거 안 함, applicability_seed_hash 추가 |
| F16 schema_version guard | §10.2 sources entry `producer / artifact_kind / supported_schema_versions` |
| F17 evidence_summary cap | §6.1.a — max 5 items, each ≤200 chars (Q4 → spec 승격) |
| F19 project-profile freshness | §3 directory layout `~/.deep-memory/projects/` + audit warning |
| F20 status_history retention | §6.3 — card 내 10 entries, 나머지 `transitions-YYYY-MM.jsonl` spill |
| F22 cross-type dedupe regression test | §12 + tests/dedupe.test.js 확장 |
| F24 Windows portability | D20 — `lock.sh` 폐기, `lock.js` 마이그레이션 |

**적용 (yellow에서 추가로 같이 끌어들임 — 옵션 C 가정 명시 포함)**:
| Finding | 적용 위치 |
|---|---|
| F18 redaction false-positive metadata | §6.1 (redaction_metadata 필드) + §11.2.a + audit 출력 |
| F21 applicability per-source provenance | §6.1 — applicability/non_applicability 항목을 `{value, source_id, confidence}` 로 |
| F23 test scope 확장 | §12 — strict YAML / Codex 1024-char / suite-extensions validation 추가 |
| F25 search_keywords (synonym) | §6.1 (필드 추가) + §7.2 (Step B emit) + §8 (FTS5 index 에 포함) |

### ℹ️ informational

| Finding | 처리 |
|---|---|
| F26 decay/recency_weight | **handoff doc 이연** (`docs/handoff-phase-4-6.md` Phase 4+) |
| F27 human discovery view | **handoff doc 이연** (단 audit `--promote` 출력에 카드 요약 포함) |
| F28 1000+ cards backpressure | **handoff doc 이연** Phase 5+ |
| F29 spec self-review false confidence | §18 보강 — false confidence 학습 명시 + handoff 진입 전 외부 리뷰 필수 |
| F30 §5 brief skill arg schema | §5 — `<task description>` 명시 (single positional, ≤2000 chars, UTF-8) |
| F31 §15 D11 해석 | §15 Phase 7 — metadata-only entry 추가임을 명시 |

## Round 2 metrics

```
round_number: 2
round_review_report_path: (R1 review reference)
response_report_path: /Users/sungmin/Dev/claude-plugins/deep-memory/.deep-review/responses/2026-05-20-132756-response.md
verdict_input: REQUEST_CHANGES (from R1)
applied_changes: 21 + 4 deferred = 25 findings addressed
implemented_count: 21
deferred_count: 4
rejected_count: 0
halted: false
execution_path: spec-edit
findings_signature_delta: 신규 issue 발생 가능성 — Round 3 (re-review) 로 검증
```

## 다음 단계 (deep-review-loop §3.B)

- §3.B 조건:
  - verdict_input ∈ (REQUEST_CHANGES, CONCERN) ✓
  - 새 변경 발생 ✓ (21 finding spec 보강)
  - max 미달 ✓ (라운드 2/5)
- → **Round 3 (Re-review)** 진입 권장. 단 모든 reviewer 재호출 cost 부담 — **Opus 1명 lightweight re-review** 로 진행 후 종료 판단.
