{
  "mcpServers": {
    "deep-memory": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/scripts/mcp-server.mjs"],
      "env": {
        "DEEP_MEMORY_ROOT": "${DEEP_MEMORY_ROOT:-${HOME}/.deep-memory}"
      }
    }
  }
}
